{% assign section_id = section.id | replace: "_", "" | replace: "-", "" %}
{% render "section-styles", section_id: section_id, section: section %}

<link href="{{ "main-activate-account.css" | asset_url }}" rel="stylesheet">

<div class="wrap wrap--medium wrap--{{ section_id }}">

    <h1>{{ 'customer.activate_account.title' | t }}</h1>

    {% form 'activate_customer_password', class: "form form--activate-account" %}

        {{ form.errors | default_errors }}

        <div class="form-row form-row--full">
            <label for="password" class="required">{{ "customer.activate_account.password_label" | t }}</label>
            <input type="password" name="customer[password]" placeholder="{{ "customer.activate_account.password_placeholder" | t }}" required="required">
        </div>
        <div class="form-row form-row--full">
            <label for="password_confirmation" class="required">{{ "customer.activate_account.confirm_password_label" | t }}</label>
            <input type="password" name="customer[password_confirmation]" placeholder="{{ "customer.activate_account.confirm_password_placeholder" | t }}" required="required">
        </div>
        <div class="form-row form-row--full form-row--actions">
            <button type="submit" class="button" aria-label="{{ 'customer.activate_account.accept_button' | t }}">{{ 'customer.activate_account.accept_button' | t }}</button>
            <button type="submit" name="decline" class="button" aria-label="{{ 'customer.activate_account.decline_button' | t }}">{{ 'customer.activate_account.decline_button' | t }}</button>
        </div>
        <input type="hidden" name="return_to" value="/account">

    {% endform %}
    
    
</div>

{% schema %}
{
    "name": "Activate Account",
    "tag": "section",
    "class": "section-activate-account",
    "settings": [
        {
            "type": "header",
            "content": "Desktop padding"
        },
        {
            "type": "range",
            "id": "padding_top",
            "min": 0,
            "max": 128,
            "step": 2,
            "unit": "px",
            "label": "Padding top",
            "default": 128
        },
        {
            "type": "range",
            "id": "padding_bottom",
            "min": 0,
            "max": 128,
            "step": 2,
            "unit": "px",
            "label": "Padding bottom",
            "default": 128
        },
        {
            "type": "header",
            "content": "Mobile padding"
        },
        {
            "type": "range",
            "id": "mobile_padding_top",
            "min": 0,
            "max": 128,
            "step": 2,
            "unit": "px",
            "label": "Padding top",
            "default": 64
        },
        {
            "type": "range",
            "id": "mobile_padding_bottom",
            "min": 0,
            "max": 128,
            "step": 2,
            "unit": "px",
            "label": "Padding bottom",
            "default": 64
        }
    ]
}
{% endschema %}