<link href="{{ "drawer-cart.css" | asset_url }}" rel="stylesheet">
<link href="{{ "cart-items.css" | asset_url }}" rel="stylesheet">

<div class="drawer drawer-cart" id="drawer-cart">
    <div class="inner">
        <div class="drawer__header drawer-cart__header">
            <div class="drawer__header__text drawer-cart__header__text">{{ "cart.drawer.title" | t }}</div>
            <button class="toggle-drawer" aria-label="Close Drawer">{% render "icon-close.svg" %}</button>
        </div>
        <div class="drawer__content drawer-cart__content">
            <p class="preorder-warning" style="border-color: {{ settings.preorder_color }};"><span>{{ settings.preorder_cart_message }}</span></p>
            <span class="loading-icon">{% render "icon-loading.svg" %}</span>
            <div class="cart-items" id="cart-items"></div>
            {% section "drawer-cart-recommended.liquid" %}
        </div>
        <div class="drawer__footer drawer-cart__footer hidden">
            <div class="cart-totals" id="cart-totals"></div>
            {% form 'cart', cart %}
            <button name="checkout" class="button button--full-width button--tall button--highlight">{{ 'cart.drawer.checkout' | t }}</button>
            {% endform %}
            {% comment %} {% render "payment-icons" %} {% endcomment %}
        </div>
    </div>
</div>
